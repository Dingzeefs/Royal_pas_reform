def new_function(parameter):
    """This is a new, improved function."""
    print(f"Executing new behavior with {parameter}...")
    # A comment related to the new function
    result = parameter * 2
    return result
